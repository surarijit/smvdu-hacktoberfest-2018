#include<stdio.h>
int main(){
int num, rev=0, rem;
printf("Enter a number:");
scanf("%d",&n);
while(num!=0)
{
rem=num%10;
rev=rev*10+rem;
num=num/10;
}
if(num==rev)
printf("This i a plaindrome number");
else
printf("This is not a plaindrome number");
return 0;
}
